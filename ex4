#include <stdio.h>
#include <string.h>

int main(){

    char str1[15];
    char str2[15];
    scanf("%s", str1);
        getchar();
    scanf("%s", str2);

    if(strstr(str1, str2))
        printf("É substring");
    else
        printf("Não é substring");
    
    return 0;
}
